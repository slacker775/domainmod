<form method="post" action="{{ path('dns_delete', {'id': dn.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dn.id) }}">
    <button style="margin-top:5px" class="btn btn-primary btn-secondary">DELETE THIS DNS PROFILE</button>
</form>
