{% extends 'base.html.twig' %}

{% block title %}
Domain Queue
{% endblock %}

{% block body %}
The Domain Queue relies on your domain registrar's API to import your
domains, so they must have an API and support for it must be built into
DomainMOD. For more information please see the
<a href="intro.php">Domain Queue information page</a>
.
<br>
<br>
<a href="/queue/add.php">{{ _self.showButton('button', 'Add Domains to Queue') }}</a>
{% if domainsInQueue %}
<a href="index.php?clear=1">{{ _self.showButton('button', 'Clear Completed') }}</a>
{% endif %}
<a href="intro.php">{{ _self.showButton('button', 'More Info') }}</a>
{% if domainsInListQueue %}
<a href="index.php?s=lists&export_data=1">{{ _self.showButton('button', 'Export Lists') }}</a>
{% endif %}
{% if domainsInQueue %}
<a href="index.php?s=domains&export_data=1">{{ _self.showButton('button', 'Export Domains') }}</a>
{% endif %}
<br>
<br>
{% endblock %}
